# Future Optional Integrations

This directory is intentionally excluded from the production runtime path.

Arus Finance core remains 100% local-first/device-owned: no login, cloud database,
server, or sync service is required for finance, backup, restore, planning, search,
or recovery. Any future cloud/multi-device integration must remain explicitly
optional and may not become a dependency of the local financial core.
